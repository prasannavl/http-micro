{
  "name": "httpx",
  "version": "1.0.0",
  "description": "Micro-framework on top of node's http module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib/*",
    "build": "npm run clean && npm run tsc",
    "tsc": "tsc",
    "watch": "tsc -w",
    "clean-examples": "rimraf examples/build/*",
    "build-examples": "npm run clean-examples && tsc --p ./examples/tsconfig.json",
    "test": "echo \"No tests available.\" && exit 0"
  },
  "keywords": [],
  "author": "Prasanna V. Loganathar <pvl@prasannavl.com> (https://www.prasannavl.com/)",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/debug": "0.0.29",
    "@types/node": "^6.0.69",
    "rimraf": "^2.6.1",
    "typescript": "^2.2.2"
  },
  "dependencies": {
    "debug": "^2.6.3"
  }
}
